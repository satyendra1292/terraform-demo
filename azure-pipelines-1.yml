trigger: none

pool:
  vmImage: ubuntu-latest

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: 'new'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: |
      az account show